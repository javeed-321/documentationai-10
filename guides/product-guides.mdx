---
title: >-
  Playground 02 — Structural components (Steps, Tabs, Expandable, Callout,
  Cards, Board)
description: >-
  Attribute matrix for layout and structural components, including icon,
  title-type, collapsed and column variations.
---
Each block has a **test ID** and an **Expected** line. Report the test ID and what actually rendered.

## Steps

### ST-01 — icons + title-type="p" (default)

Expected: three numbered steps, each with its Lucide icon, titles rendered as paragraph text.

<Steps>
  <Step title="Install dependencies" icon="download" title-type="p">
    Run the installation command to add required packages.

    ```bash
    npm install documentation-ai
    ```
  </Step>

  <Step title="Configure environment" icon="settings" title-type="p">
    Create a `documentation.json` file with your site configuration.

    ```c wrap="true"
    { "name": "Capillary Docs", "initialRoute": "docs/introduction" }
    ```
  </Step>

  <Step title="Start development server" icon="play" title-type="p">
    Launch the local development server with `npm run dev`.
  </Step>
</Steps>

### ST-02 — title-type="h2" and "h3"

Expected: step titles appear in the right-hand page table of contents as headings. Report whether the TOC picks them up.

<Steps>
  <Step title="Heading level two step" icon="hash" title-type="h2">
    This title should be an `<h2>` and appear in the on-page TOC.
  </Step>

  <Step title="Heading level three step" icon="hash" title-type="h3">
    This title should be an `<h3>`, nested under the previous entry in the TOC.
  </Step>
</Steps>

### ST-03 — camelCase `titleType` instead of kebab-case

Expected: the CLAUDE.md reference shows both spellings. If `titleType` silently does nothing while `title-type` works, that inconsistency is worth reporting.

<Steps>
  <Step title="camelCase attribute test" icon="alert-triangle" title-type="h3">
    Does this render as an h3 like ST-02, or fall back to a paragraph?
  </Step>
</Steps>

### ST-04 — no icon, no title-type

Expected: step numbers still render; no gap or placeholder where the icon would be.

<Steps>
  <Step title="Bare step one" title-type="p">
    Only a title was supplied.
  </Step>

  <Step title="Bare step two" title-type="p">
    Only a title was supplied here too.
  </Step>
</Steps>

### ST-05 — invalid icon name

Expected: graceful fallback. Report if this throws, renders a broken glyph, or blanks the step.

<Steps>
  <Step title="Icon that does not exist" icon="this-icon-does-not-exist" title-type="p">
    Testing the fallback path for an unknown Lucide icon name.
  </Step>
</Steps>

### ST-06 — heavy content inside a single step

Expected: the code group, callout, table and nested list all render inside step 1's body, correctly indented.

<Steps>
  <Step title="A step containing everything" icon="layers" title-type="p">
    Leading paragraph inside the step.

    <CodeGroup show-lines="true">
      ```bash
      npm run build
      ```

      ```json
      { "ok": true }
      ```
    </CodeGroup>

    | Parameter | Type    | Description                 |
    | --------- | ------- | --------------------------- |
    | api\_key  | string  | Your API authentication key |
    | timeout   | integer | Request timeout in seconds  |

    - Outer list item
      - Nested list item

    <Callout kind="success" collapsed="false">
      Verify the installation by running `npm list documentation-ai`.
    </Callout>
  </Step>

  <Step title="The step after the heavy one" icon="check" title-type="p">
    Numbering must continue as 2 — report if it resets to 1.
  </Step>
</Steps>

## Tabs

### TB-01 — icons on each tab

Expected: three tabs with apple / monitor / terminal icons, first one active.

<Tabs>
  <Tab title="macOS" icon="apple">
    ```bash
    brew install documentation-ai
    ```
  </Tab>

  <Tab title="Windows" icon="monitor">
    ```powershell
    winget install documentation-ai
    ```
  </Tab>

  <Tab title="Linux" icon="terminal">
    ```bash
    sudo apt install documentation-ai
    ```
  </Tab>
</Tabs>

### TB-02 — many tabs (overflow)

Expected: the tab strip scrolls or wraps; it must not push the page into horizontal scroll.

### TB-03 — tabs without icons, mixed content

Expected: table renders inside tab 1, image inside tab 2, no layout shift when switching.

<Tabs>
  <Tab title="Table tab">
    | Field    | Type    | Required |
    | -------- | ------- | -------- |
    | name     | string  | yes      |
    | priority | integer | no       |
  </Tab>

  <Tab title="Image tab">
    <Image src="https://files.readme.io/f1f2d3a-password_validate.jpg" width="439" height="610" alt="Flow chart illustrating the password validation steps" />
  </Tab>
</Tabs>

## Expandable and ExpandableGroup

### EX-01 — default-open false vs true

Expected: the first is collapsed on load, the second is already open.

<ExpandableGroup>
  <Expandable title="Closed by default" default-open="false">
    - Ensure your API key is valid and not expired
    - Check firewall settings allow outbound connections
  </Expandable>

  <Expandable title="Open by default" default-open="true">
    ```javascript
    const advancedConfig = {
      retryAttempts: 3,
      caching: { enabled: true, ttl: 3600 }
    };
    ```
  </Expandable>
</ExpandableGroup>

### EX-02 — default-open omitted

Expected: collapsed (documented default is `false`).

<Expandable title="No default-open attribute supplied" default-open="false">
  This should start closed.
</Expandable>

### EX-03 — JSX boolean form

Expected: identical behaviour to `default-open="true"`.

<Expandable title="default-open as a JSX expression" default-open="true">
  This should start open.
</Expandable>

### EX-04 — standalone Expandable outside a group

Expected: renders correctly on its own; report any missing border or spacing collapse compared with EX-01.

<Expandable title="Standalone expandable" default-open="false">
  Content that lives outside an `ExpandableGroup`.
</Expandable>

## Callout

### CO-01 — all five kinds

Expected: five distinct colours and icons: info, tip, alert, danger, success.

<Callout kind="info" collapsed="false">
  Supplementary information that supports the main content without interrupting flow.
</Callout>

<Callout kind="tip" collapsed="false">
  Expert advice, shortcuts, or best practices that enhance user success.
</Callout>

<Callout kind="alert" collapsed="false">
  Critical information about potential issues, breaking changes, or actions requiring attention.
</Callout>

<Callout kind="danger" collapsed="false">
  Warnings about destructive actions, data loss, or irreversible operations.
</Callout>

<Callout kind="success" collapsed="false">
  Positive confirmations, successful completions, or achievement indicators.
</Callout>

### CO-02 — collapsed="true"

Expected: renders collapsed with a toggle. Report whether `collapsed` does anything at all.

<Callout kind="info" collapsed="true">
  This callout was marked `collapsed="true"`. Does it start collapsed and expand on click?
</Callout>

### CO-03 — unknown kind value

Expected: graceful fallback to a neutral style rather than an error or an unstyled block.

<Callout kind="warning" collapsed="false">
  `warning` is not in the documented kind list — `alert` is. What happens?
</Callout>

### CO-04 — no kind attribute

Expected: a default style, not a crash.

<Callout kind="note" collapsed="false">
  No `kind` was supplied here.
</Callout>

### CO-05 — rich content inside a callout

Expected: heading, list, code block, table and link all render inside the callout box, not spilling below it.

<Callout kind="alert" collapsed="false">
  **Migration required before 2026-09-01**

  - Old `v1` endpoints stop responding
  - Tokens must be reissued

  ```bash
  npx capillary-migrate --from v1 --to v2
  ```

  | Endpoint       | Replacement     |
  | -------------- | --------------- |
  | `/v1/customer` | `/v2/customers` |

  See the [v2 migration guide](/docs/event-schema-payload) for the full checklist.
</Callout>

## Cards and Columns

### CD-01 — horizontal false vs true

Expected: the first card stacks icon above title, the second lays them out side by side.

<Card title="Stacked card" href="/docs/event-schema-payload" icon="rocket" horizontal="false">
  Complete walkthrough from installation to your first deployment in under 10 minutes.
</Card>

<Card title="Horizontal card" href="/docs/event-schema-payload" icon="rocket" horizontal="true">
  Same content, `horizontal="true"`. The layout should differ visibly from the card above.
</Card>

### CD-02 — horizontal as a JSX boolean

Expected: identical to `horizontal="true"` in CD-01.

<Card title="Horizontal via JSX expression" href="/docs/event-schema-payload" icon="settings" horizontal="true">
  Report any difference between `horizontal={true}` and `horizontal="true"`.
</Card>

### CD-03 — Columns at cols 2, 3 and 4

Expected: 2, then 3, then 4 cards per row on desktop; all collapse to one column on mobile.

<Columns cols="2">
  <Card title="Components" href="/docs/event-schema-payload" icon="component" horizontal="false">
    Two-column grid, card one.
  </Card>

  <Card title="API Reference" href="/docs/event-schema-payload" icon="code" horizontal="false">
    Two-column grid, card two.
  </Card>
</Columns>

<Columns cols="3">
  <Card title="Fast setup" href="#" icon="zap" horizontal="false">
    Get started in minutes with zero configuration.
  </Card>

  <Card title="Full control" href="#" icon="settings" horizontal="false">
    Customise every aspect of your documentation.
  </Card>

  <Card title="Team ready" href="#" icon="users" horizontal="false">
    Built for collaboration and scale.
  </Card>
</Columns>

### CD-04 — odd card count in an even grid

Expected: the last card should not stretch oddly across the full row. Report how the orphan is handled.

### CD-05 — Columns wrapping plain content in divs

Expected: two side-by-side text columns of equal width.

### CD-06 — Columns containing non-Card, non-div content

Expected: this is the documented failure mode — CLAUDE.md says plain content needs a `<div>` wrapper. Confirm whether raw content actually breaks.

### CD-07 — card with a very long title and no description

Expected: the title wraps inside the card; card heights in the row stay aligned.

<Columns cols="2">
  <Card title="A deliberately long card title that should wrap onto at least two lines to test vertical alignment" href="#" icon="text" horizontal="false">

  </Card>

  <Card title="Short" href="#" icon="minus" horizontal="false">

  </Card>
</Columns>

## Board

### BD-01 — full attribute set

Expected: three columns with their colours and icons; cards show description, due date, author and created date.

<Board uid="df3ac9be-81a6-4663-ac98-b6efb165c4fe" title="Capillary product roadmap">
  <BoardColumn title="Planned" uid="4e05bf00-fd2e-4c71-a78d-25a0e790200e" color="3" icon="check-circle">
    <BoardCard title="Webhook support" uid="1dadcdb2-0da0-4017-9691-b6112f429c24" description="Outbound events for loyalty transactions" icon="webhook" due-date="2026-02-01" author="Platform team" created-at="2026-01-15" />

    <BoardCard title="Bulk export API" uid="351848bd-c057-4ed4-8f8b-34135102487d" description="CSV and Parquet export for fact tables" icon="download" due-date="2026-03-15" />
  </BoardColumn>

  <BoardColumn title="In development" uid="f10e0fb4-da08-450e-8402-3929610e4f71" color="5" icon="play-circle">
    <BoardCard title="Dark mode" uid="9c796064-6b6c-4f5c-b49d-d1e80eab50cb" author="Design team" created-at="2026-01-02" />
  </BoardColumn>

  <BoardColumn title="Shipped" uid="fe2ee5b6-5c02-4ca6-a115-4893d80cdaae" color="4" icon="rocket">
    <BoardCard title="SSO with SAML" uid="ffcb8f93-0145-40ac-acf0-350b4c8ae841" description="Okta and Azure AD verified" />
  </BoardColumn>
</Board>

### BD-02 — every colour index 0 through 8

Expected: nine visibly different column colours (gray, brown, orange, yellow, green, blue, purple, pink, red). Report any duplicates or missing colours, and check contrast in dark mode.

<Board uid="88a8b975-4116-4d8c-86d3-ca535f813365" title="Colour index sweep">
  <BoardColumn title="0 gray" uid="f7392d8f-ab3b-480c-8ee2-be39333ca1fb" color="0">
    <BoardCard title="Card" uid="0b87c51d-6af9-41df-8382-d7aa7624859a" />
  </BoardColumn>

  <BoardColumn title="1 brown" uid="190916c1-ac95-4804-a209-27f04140d2e0" color="1">
    <BoardCard title="Card" uid="2c9d8121-4271-4dc5-b731-c845d4597a13" />
  </BoardColumn>

  <BoardColumn title="2 orange" uid="912e4491-bde9-4778-8274-2b6823432804" color="2">
    <BoardCard title="Card" uid="35086d27-3a43-4d53-9a8c-2542137a5bcd" />
  </BoardColumn>

  <BoardColumn title="3 yellow" uid="7b68887a-1cb4-481a-b586-746085cbe1ae" color="3">
    <BoardCard title="Card" uid="bc12eb12-f2e4-46ea-b73d-477b10666dbf" />
  </BoardColumn>

  <BoardColumn title="4 green" uid="9ed82edb-5e11-43ef-88f0-53fbbbda3f18" color="4">
    <BoardCard title="Card" uid="7c24f02e-94c8-4737-96ef-b290ee3795f8" />
  </BoardColumn>

  <BoardColumn title="5 blue" uid="0582d75f-d719-484d-bcb1-cfca314cd764" color="5">
    <BoardCard title="Card" uid="371e9d7b-2886-41d8-832e-35d7f165400e" />
  </BoardColumn>

  <BoardColumn title="6 purple" uid="df7a715c-893b-4188-8aac-59bc73a9e259" color="6">
    <BoardCard title="Card" uid="c1092822-c00f-47f8-969b-b6a8876418d0" />
  </BoardColumn>

  <BoardColumn title="7 pink" uid="ac0f09c2-1730-48ff-bcb0-bd17b91d33f8" color="7">
    <BoardCard title="Card" uid="1302c320-94cb-4969-ac46-f7c5fff69fd6" />
  </BoardColumn>

  <BoardColumn title="8 red" uid="4b69373d-1c6b-4677-a5b3-0d499d12b71d" color="8">
    <BoardCard title="Card" uid="24a28d21-da45-4c4d-9984-302fdc127ee0" />
  </BoardColumn>
</Board>

### BD-03 — minimal board, out-of-range colour, empty column

Expected: `title` omitted on `Board` falls back to "Board"; `color="12"` degrades gracefully; the empty column still renders its header.

<Board uid="2d4d4079-ab17-4744-bbb9-7975383c6045" title="Board">
  <BoardColumn title="Out of range colour" uid="b1c2eaac-5831-464e-bbd0-d6b3d53ea884" color="12">
    <BoardCard title="Colour index 12 does not exist" uid="766bee3a-b06a-4736-9b6d-189772bb635d" />
  </BoardColumn>

  <BoardColumn title="Empty column" uid="40894135-1550-499c-9a79-0464ae35d761" color="0" icon="inbox" />
</Board>

